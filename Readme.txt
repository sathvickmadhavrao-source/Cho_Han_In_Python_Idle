First thing happening in the program is that I'm importing two functions here, one called random(which can randomise a list, dictionary, strings or numbers(int or float)) and the other called time(To skip some time, add an interval so that the game feels well-paced in real time instead of executing codes instantly). I'm making a list first, the die_numbers in which I store all the possible outcomes that may occur when you roll a dice. Then I create another variable called game_continue and give it the Boolean value of True and a variable called score and comp_score which keeps score of the both the user and the computer's wins(or losses, yeah there's negative marking here). 
Opening the while loop now and keeping the condition of it as game_continue==True, meaning it will execute as long as game_continue is True. First off I take the input from the user over whether they want to start or continue playing the game, keeping y or n as two elif statements, and another else statement which executes if they chose neither 'y' or 'n'. 
I make two dice and make the random.choice function give them two random values from the die_numbers list. Then I combine the sum of those two dice. Now whether these two dice are even or odd is what determines the score of the computer's and that of the user. 
I used the time.sleep() function to skip time here, the number inside the parentheses of the function is how many seconds are skipped here.
I start making elif statements over conditions. First condition is when the user chose even and the roll was odd, likewise, when the user chose odd and the roll was even, when the user chose even and the roll was even, when the user chose odd and the roll was odd. For every correct guess, user gets a point and for every wrong guess, they lose one. I did the same for the computer's choice too, though it's a randomised number turned into a choice. I used the random.randint() function which gives me any random number between the number a and the number b which is to be specified in the parentheses, that variable is called choice_of_comp as you can see. If it's one, then I make the program assume that the computer "chose" odd while if it's two, then I make the program assume that it "chose" even. I'm basically manipulating the compiler to think that random numbers are choices, sort of like real life. 
Then when the user finally chooses 'n', meaning they stop wanting to continue the program, I use more elif statements to compare user's score with the computer's score, if it's equal, it's a draw, if it's not, then whose got more score wins. 